import 'package:flutter/material.dart';
import 'package:flutter_application_5/Screens/BluetoothPage/components/body.dart';

class Bluetoothpage extends StatelessWidget {
  const Bluetoothpage({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Body(),
    );
  }
}
